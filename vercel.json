{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.34.1 --activate && pnpm install --no-frozen-lockfile",
  "buildCommand": "corepack enable && corepack prepare pnpm@10.34.1 --activate && pnpm run build",
  "outputDirectory": "out"
}
