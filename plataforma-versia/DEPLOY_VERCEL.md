# Deploy na Vercel (descontinuado para o backend)

O backend Django **não** é mais implantado na Vercel (incompatibilidades com `django-tenants`, `uv` e serverless).

Use **[DEPLOY_RENDER.md](./DEPLOY_RENDER.md)** para o backend e mantenha o **frontend** na Vercel (`plataforma-versia/frontend`).
