# URL pública do backend Django (projeto Vercel separado)
NEXT_PUBLIC_API_URL=https://seu-backend.vercel.app

# Schema do tenant (empresa) — usado nas chamadas com header X-Tenant
NEXT_PUBLIC_TENANT_SCHEMA=demo
